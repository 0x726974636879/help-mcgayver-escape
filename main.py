import pygame

from constants import (
	WINDOW_SIZE
)

# Initialize pygame
pygame.init()
# Create the window
window = pygame.display.set_mode(WINDOW_SIZE)
# Condition loop for the game continue
play = True

while play:
	continue
